mixin carousel-book(book)
  li
    .carousel-card
      img.carousel-cover(src='./img/#{book.id}.jpg')
      span.carousel-time #{book.length} hrs.

mixin carousel-section(title, books)
  - var titleClass = title.toLowerCase().replace(" ","-")
  .container
    .row.book-showcase.quick-reads
      div(class = "col-xs-12 list-title list-#{titleClass}")
        h6.section-title
          | #{title}
          span.tag.tag-default.float-xs-right See All
      .col-xs-12.carousel
          ul
            for book in books
              +carousel-book(book) 

mixin search-results(searchTerm, books)
  .container
    .search-results-bar
      h4 Search Results for 
        span.search-term "#{searchTerm}"
      nav(aria-label='Page navigation ')
        ul.pagination.pagination-sm
          li.page-item.disabled
            a.page-link(href='#', aria-label='Previous')
              span(aria-hidden='true') «
              span.sr-only Previous
          li.page-item.active
            a.page-link(href='#') 1
          li.page-item
            a.page-link(href='#', aria-label='Next')
              span(aria-hidden='true') »
              span.sr-only Next
    .search-results
      ul.container.search-container
        //- Create search cards
        for book in books
          li.search-card
            .search-details-cover
              img(src='./img/#{book.id}.jpg')
              p.details-link
                a(href='#') More Details
            .search-details-info
              h5.search-details-title #{book.title}
              h6.search-details-author #{book.author}
              .search-details-time
                p.search-details-time-title Expected Reading Time: 
                p.search-details-time-amount #{book.length} Hours and 24 Minutes
              img.amazon-banner-01(src='./img/icons/amazon-banner-01.gif')
            .search-details-table
              table
                for detail in ["Year","Pages","Words","Genre"]
                  tr
                    th #{detail}
                    td #{book[detail.toLowerCase()]}
                    
